server.port: 7002
server.error.include-message: always
spring:
  graphql:
    schema:
      locations: classpath*:graphql/recommendation-service/**/
    graphiql:
      path: /graphiql
      enabled: true

spring.data.mongodb:
  host: localhost
  port: 27017
  database: recommendation-db

logging:
  level:
    root: INFO
    se.magnus: DEBUG
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG

---
spring.config.activate.on-profile: docker
spring:
  graphql:
    schema:
      locations: classpath*:graphql/recommendation-service/**/
    graphiql:
      path: /graphiql
      enabled: true

server:
  port: 7002

spring.data.mongodb.host: mongodb